import React from 'react'


const CustomButton = ({text}) => {
    return (
        <>
                        <div style={{borderRadius:30}} className=' px-4 bg-primary pt-2 pb-2  btn border-0 shadow-none  text-white '>{text}</div>

        </>
    )
}

export default CustomButton
